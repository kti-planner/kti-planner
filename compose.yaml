name: kti-planner

services:
  web:
    build:
      context: .
    depends_on:
      - postgres
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      POSTGRES_HOST: postgres
  postgres:
    build:
      context: ./db
    env_file:
      - .env
